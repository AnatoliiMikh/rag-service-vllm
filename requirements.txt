# async runtime
anyio>=4.0.0

# OpenAI-compatible clients (vLLM, embedding, reranker)
openai>=1.40.0

# HTTP client for reranker / custom endpoints
httpx>=0.27.0

# Qdrant client (hybrid retrieval)
qdrant-client>=1.9.0

# BM25 + sparse embeddings (FastEmbed BM25)
fastembed>=0.3.6

# environment variables
python-dotenv>=1.0.1

# typing + utilities
pydantic>=2.7.0
typing-extensions>=4.10.0