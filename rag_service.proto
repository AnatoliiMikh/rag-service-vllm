syntax = "proto3";

package LLMWorkerAPI;

service MessageService {
  rpc GenerateReply (NewMessageRequest) returns (NewMessageResponse);
}

message NewMessageRequest {
  repeated string messages = 1;  // messages[0] = current user message, messages[1:] = history
}

message NewMessageResponse {
  string answer = 1;
}